{
   int num;
   int current;
   int last;
   int count;
   num := 10;
   current := 1;
   last := 1;
   count := 1;
   while (count < num)
    {
       int next;
       next := (current + last);
       last := current;
       current := next;
       count := (count + 1);
    }
}